# Copy to .env and fill in values. Never commit .env.

# FastAPI base URL for Streamlit (default matches local uvicorn)
BACKEND_URL=http://127.0.0.1:8000

# Alibaba DashScope API key for Qwen (optional until LLM step)
DASHSCOPE_API_KEY=
